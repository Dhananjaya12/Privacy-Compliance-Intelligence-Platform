[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# test_5_integration.py is skipped unless credentials present — mark it
markers =
    integration: marks tests that require live Azure/Neo4j credentials

# Show print() output by default
addopts = -s
